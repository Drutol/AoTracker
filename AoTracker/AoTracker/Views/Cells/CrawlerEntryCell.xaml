<?xml version="1.0" encoding="UTF-8"?>

<ContentView xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:forms="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:util="clr-namespace:AoTracker.Util;assembly=AoTracker"
             xmlns:xamEffects="clr-namespace:XamEffects;assembly=XamEffects"
             xmlns:controls="clr-namespace:AoTracker.Controls;assembly=AoTracker"
             mc:Ignorable="d"
             Padding="8"
             x:Class="AoTracker.Views.Cells.CrawlerEntryCell">

    <d:ContentView.BindingContext>
        <x:Static Member="util:DesignTimeBindingContextProvider.CrawlerEntryViewModel" />
    </d:ContentView.BindingContext>


    <controls:FocusableFrame HasShadow="True"
           Padding="0"
           BackgroundColor="{DynamicResource ForegroundColour}"
           HeightRequest="100" Command="{Binding SelectCommand}" CommandParameter="{Binding BackingModel}">
        <ContentView Padding="8,16">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <forms:CachedImage Source="{Binding BackingModel.ImageSource}" />
                <Label Grid.Row="1" Text="{Binding BackingModel.Title}" HorizontalOptions="Center" />
            </Grid>
        </ContentView>
    </controls:FocusableFrame>


</ContentView>